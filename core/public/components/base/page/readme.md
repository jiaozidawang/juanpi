## page
```
/**
 * Y表示必填,N表示非必填
 * @desc - 页面布局,最多分为三份，head,main,footer, head和footer可以指定高度,main部份自动适应减少head和footer的高度
 * @props { namespace: String } N 指定名命名空间, 默认读取当前的url的一部份来生成
 * @example 
 *		<m-page namespace="xxx"></m-page>
 *          
 *
 * ps 自适应的部份使用 main 作为class
 */
 ```
